import { React } from 'react';

const Meals = () => {
    // const { user } = useUser();
    // const { items: foods, add, remove, clear } = useItemCollector('mealFoods');
    // const { results, error, searchByName, searchByBarcode } = useFoodSearch();
    // const [servings, setServings] = useState(1);
    // const [size, setSize] = useState('1');
    // const [mealType, setMealType] = useState('breakfast');

return (
    <h1>Coming Soon</h1>
);
}

export default Meals;